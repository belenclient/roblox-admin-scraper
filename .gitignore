# Runtime state & logs (regenerated on every run)
state.json
scraper_log.txt
config.json

# Profile checker runtime files
checker_progress.txt
checker_log.txt
chrome_profile/

# Profile checker tool - local-only, never uploaded (per owner request)
check_profiles.bat
check_profiles.py

# Python
__pycache__/
*.pyc